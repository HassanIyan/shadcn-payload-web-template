import type { Block } from 'payload'

export const TimelineOne: Block = {
    slug: 'timeline-one',
    labels: {
        singular: 'Timeline',
        plural: 'Timelines',
    },
    admin: {
        group: 'Timeline',
    },
    fields: [],
}
