import type { Block } from 'payload'

export const HeroTwo: Block = {
    slug: 'hero-two',
    labels: {
        singular: 'Gradient Hero',
        plural: 'Gradient Heros',
    },
    admin: {
        group: 'Hero',
    },
    fields: [],
}
