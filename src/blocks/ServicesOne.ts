import type { Block } from 'payload'

export const ServicesOne: Block = {
    slug: 'services-one',
    labels: {
        singular: 'Service',
        plural: 'Services',
    },
    admin: {
        group: 'Services',
    },
    fields: [],
}
