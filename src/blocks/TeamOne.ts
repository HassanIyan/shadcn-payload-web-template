import type { Block } from 'payload'

export const TeamOne: Block = {
    slug: 'team-one',
    labels: {
        singular: 'Team Listing',
        plural: 'Team Listings',
    },
    admin: {
        group: 'Team',
    },
    fields: [],
}
