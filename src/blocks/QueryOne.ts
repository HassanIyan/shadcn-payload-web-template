import type { Block } from 'payload'

export const QueryOne: Block = {
    slug: 'query-one',
    labels: {
        singular: 'News Query',
        plural: 'News Queries',
    },
    admin: {
        group: 'Query',
    },
    fields: [],
}
