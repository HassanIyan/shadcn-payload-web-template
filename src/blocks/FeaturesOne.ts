import type { Block } from 'payload'

export const FeaturesOne: Block = {
    slug: 'features-one',
    labels: {
        singular: 'Icon Card',
        plural: 'Icon Cards',
    },
    admin: {
        group: 'Features',
    },
    fields: [],
}
