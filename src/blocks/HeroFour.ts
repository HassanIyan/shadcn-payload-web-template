import type { Block } from 'payload'

export const HeroFour: Block = {
    slug: 'hero-four',
    labels: {
        singular: 'Map Hero',
        plural: 'Map Heros',
    },
    admin: {
        group: 'Hero',
    },
    fields: [],
}
