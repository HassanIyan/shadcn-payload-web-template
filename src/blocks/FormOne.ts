import type { Block } from 'payload'

export const FormOne: Block = {
    slug: 'form-one',
    labels: {
        singular: 'Form',
        plural: 'Forms',
    },
    admin: {
        group: 'Form',
    },
    fields: [],
}
