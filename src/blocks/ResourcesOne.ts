import type { Block } from 'payload'

export const ResourcesOne: Block = {
    slug: 'resources-one',
    labels: {
        singular: 'Resource',
        plural: 'Resources',
    },
    admin: {
        group: 'Resources',
    },
    fields: [],
}
