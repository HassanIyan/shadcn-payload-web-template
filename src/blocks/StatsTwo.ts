import type { Block } from 'payload'

export const StatsTwo: Block = {
    slug: 'stats-two',
    labels: {
        singular: 'Clean Stat',
        plural: 'Clean Stats',
    },
    admin: {
        group: 'Stats',
    },
    fields: [],
}
